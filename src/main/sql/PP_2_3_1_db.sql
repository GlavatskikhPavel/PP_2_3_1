create table user
(
    id    int PRIMARY KEY AUTO_INCREMENT NOT NULL,
    name  varchar(20)                    not null,
    age   int                            not null,
    email varchar(40)                    not null
);